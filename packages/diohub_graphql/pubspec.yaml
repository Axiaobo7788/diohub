name: diohub_graphql
description: GraphQL schema, queries, and type definitions for DioHub GitHub client
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  json_annotation: ^4.12.0
  gql: ^1.0.1

dev_dependencies:
  build_runner: ^2.15.2
  graphql_codegen: ^3.0.2
  json_serializable: ^6.14.0
