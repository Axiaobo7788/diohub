query accountTier($login: String!) {
  user(login: $login) {
    sponsorshipForViewerAsSponsor {
      createdAt
      isActive
      isOneTimePayment
      tier {
        id
        name
        monthlyPriceInDollars
        description
      }
    }
  }
}
