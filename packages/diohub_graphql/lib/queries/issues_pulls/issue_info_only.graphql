# import '../../fragments/issue_detail_fields.graphql'

query issueInfoOnly($owner: String!, $name: String!, $number: Int!) {
  repository(name: $name, owner: $owner) {
    issue(number: $number) {
      ...issueDetailFields
    }
  }
}
