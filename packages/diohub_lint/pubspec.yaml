name: diohub_lint
description: Custom lint rules for DioHub architecture enforcement.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  analyzer: ">=6.0.0 <12.0.0"
  analyzer_plugin: ">=0.11.0 <1.0.0"
  custom_lint_builder: ^0.8.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
