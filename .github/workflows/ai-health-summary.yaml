name: Weekly Health Summary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-private:
    uses: namanshergill/diohub-premium/.github/workflows/_ai-health-summary.yaml@main
    secrets: inherit
